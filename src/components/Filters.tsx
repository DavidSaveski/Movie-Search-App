import "../styles/FilterSection.css";

export default function Filters() {
  return (
    <section className="filters-grid">
      <div className="panel">
        Search Results
        <ul>Filters</ul>
      </div>
    </section>
  );
}
